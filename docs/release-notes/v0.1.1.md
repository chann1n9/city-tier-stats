# v0.1.1

## 重点变化

- 调整 Windows 安装包右键菜单的执行命令，优化从资源管理器触发分析时的命令行调用方式。

## 变更

- 更新 `installer/windows/installer.iss` 中 `CityTierStats` 右键菜单命令配置。
- 项目版本从 `0.1.0` 升级为 `0.1.1`。

## 安装包

- Windows: `city-tier-stats-0.1.1-setup.exe`
- macOS: `city-tier-stats-0.1.1.pkg`
